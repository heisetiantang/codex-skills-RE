# 技能速查

这张表只回答一个问题：**我要干这件事，该用哪个技能。**

- 调用方式：对话里打 `/技能名`，或直接描述意图让它自动匹配。
- 「自动」列 ✅ = 描述意图就能命中；❌ = 必须手动打 `/技能名`。
- 权威清单以 `skills-manifest.json` 为准，本文件是给人看的入口。
- ✅❌ 之外的第三个标记：⚠️ = 技能能用，但依赖没配就是废物。

---

## 一、按场景索引

### 学习与调研

| 技能 | 一句话作用 | 依赖 / 前提 | 自动 |
| --- | --- | --- | --- |
| `learn-anything-fast` | 把学习目标变成可测闭环：路线图 → 揪出核心 20% → 单题测验 → 错题诊断 → 一页速查 → Feynman 复述 | 无，纯提示词 | ✅ |
| `arxiv-digest` | 每天抓 arXiv 新论文，按预设 profile 批量打分后推送 | ⚠️ `.env` 里需 `MASTODON_ACCESS_TOKEN`、`DISCORD_WEBHOOK_URL_*`；提示词是**日文** | ✅ |
| `spatial-agi-research` | Spatial AGI 领域专用流水线：搜 arXiv → 每日精读 5 篇 → NotebookLM 提 3 个核心问题 → 出论文文档 + 每日思考 | ⚠️ 需 NotebookLM，或备选 GLM WebReader MCP；**领域写死为 Spatial AGI**；1881 行，全库最重 | ✅ |

### 写代码

| 技能 | 一句话作用 | 依赖 / 前提 | 自动 |
| --- | --- | --- | --- |
| `context7-mcp` | 查库/框架的**真实最新文档**，防止模型凭记忆编 API | ⚠️ 需 Context7 MCP 已连接 | ✅ |
| `implement` | 按 spec / ticket 落地功能实现 | 无 | ❌ 只能手动 |
| `tdd` | 红 → 绿 → 重构，附带"什么样的测试值得留"的判断标准 | 无 | ✅ |
| `code-review` | 并行双轴审 diff：**规范**（合不合本仓库既有规范）+ **需求**（实现了原 spec 没有） | 需你给一个基点（commit / branch / tag） | ✅ |
| `diagnosing-bugs` | 疑难 bug 和性能回归的定位纪律：先建可复现回路，再收敛 | 无 | ✅ |

### 机械 / 工程

| 技能 | 一句话作用 | 依赖 / 前提 | 自动 |
| --- | --- | --- | --- |
| `solidworks-automation` | Python COM 驱动 Windows 上的 SolidWorks：零件建模、装配体、工程图、钣金、焊件、仿真、文件导出 | ⚠️ Windows + SolidWorks 已运行 + `pywin32` / `comtypes` | ✅ |

### 网页数据（三个重叠）

| 技能 | 一句话作用 | 依赖 / 前提 | 自动 |
| --- | --- | --- | --- |
| `firecrawl-build` | 在**应用代码里**集成 Firecrawl 的总纲（网搜 / 抓取 / 结构化抽取 / 浏览器交互） | ⚠️ `FIRECRAWL_API_KEY` | ✅ |
| `firecrawl-build-search` | 子技能，只讲 `/search` 一个接口 | ⚠️ 同上 | ✅ |
| `firecrawl-build-scrape` | 子技能，只讲 `/scrape` 一个接口 | ⚠️ 同上 | ✅ |

> 三个都定位在「帮开发者写集成代码」，不是「帮你抓个网页看」。若只是临时取一个页面正文，直接让我抓就行，不用动用它们。

### 安全检查

| 技能 | 一句话作用 | 依赖 / 前提 | 自动 |
| --- | --- | --- | --- |
| `semgrep` | 快速扫常见安全问题，输出 SARIF | ⚠️ 需 Semgrep CLI | ✅ |
| `codeql` | 跨文件数据流 / 污点分析找漏洞，覆盖 9 种语言 | ⚠️ 需 CodeQL CLI + `jq` | ✅ |

### 其他

| 技能 | 一句话作用 | 依赖 / 前提 | 自动 |
| --- | --- | --- | --- |
| `goutoujunshi` | 恋爱军师与情绪支持：关系信号分析、推进/退出策略、话术润色 | 无 | ✅ |
| ~~`jev-chat-jarvis`~~ | **不是技能**——Android 应用源码（Kotlin / Gradle），无 SKILL.md | — | — |

---

## 二、取舍建议

`skills/` 现有 16 个目录。按「你手上有对应交付物吗」过一遍：

| 技能 | 建议 | 理由 |
| --- | --- | --- |
| `learn-anything-fast` | **留** | 直接对口 12 周自学计划，且零依赖 |
| `context7-mcp` | **留** | 写脚本、查 API 的高频动作，收益稳定 |
| `solidworks-automation` | **留** | 机械背景唯一对口的 CAD 自动化，别人替代不了 |
| `implement` / `tdd` / `code-review` / `diagnosing-bugs` | **留** | 同一套开发流程四件套，互相配套，拆开就没意义 |
| `arxiv-digest` | **留但先配** | 调研有用；不配 `.env` 就是死技能，且输出面向日本推送渠道 |
| `spatial-agi-research` | **留但先改领域** | SLAM / 视觉调研对口，但领域写死，换方向要改提示词 |
| `firecrawl-build*` ×3 | **三合一** | 同一工具的三个粒度，触发条件重叠，保留 `firecrawl-build` 即可 |
| `semgrep` / `codeql` | **归档** | 面向 Web/安全交付；你当前是 POC 需求与方案，用不上 |
| `goutoujunshi` | **你定** | 与工作无关，与技能库定位不符（若留着，建议移出 `skills/` 单放） |
| `jev-chat-jarvis` | **移出** | 不是技能，放错位置，还占 6.8 MB |

按此收敛：**16 → 约 10 个**，且每个都能一句话说出"什么时候用它"。

---

## 三、需要先配好的东西

| 依赖 | 被谁用 | 怎么配 |
| --- | --- | --- |
| `FIRECRAWL_API_KEY` | firecrawl 三件套 | firecrawl.dev 后台取 key，写进项目 `.env` |
| Semgrep CLI | `semgrep` | `semgrep --version` 自检，未装见 semgrep.dev 安装页 |
| CodeQL CLI + `jq` | `codeql` | `gh extension install github/gh-codeql`；jq 单独装 |
| `pywin32` | `solidworks-automation` | `pip install pywin32` |
| Context7 MCP | `context7-mcp` | 在 MCP 配置里加 context7 server 并信任 |
| `.env` 凭证 | `arxiv-digest` | 配 `MASTODON_ACCESS_TOKEN`、`DISCORD_WEBHOOK_URL_*` |
| NotebookLM / GLM WebReader MCP | `spatial-agi-research` | 二选一，NotebookLM 不可用时切 MCP |

---

## 四、常用组合

| 我要干的事 | 套路 |
| --- | --- |
| 学一个新东西 | `learn-anything-fast`（路线 → 测验 → 速查） |
| 写一个新功能 | `implement` → `tdd` → `code-review` |
| 排查一个查不动的 bug | `diagnosing-bugs` → `tdd` 补回归测试 |
| 动手前确认 API 写法 | 先 `context7-mcp`，再动手 |
| 摸一个领域的最新进展 | `arxiv-digest` 看日刊，`spatial-agi-research` 深挖 |
| CAD 批量出图 / 改模型 | `solidworks-automation` |
